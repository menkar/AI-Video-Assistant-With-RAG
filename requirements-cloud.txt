# =============================================================================
#  requirements-cloud.txt — Render / cloud deployment (memory-optimised)
#
#  Excludes torch, whisper, numba, llvmlite, torchaudio — these require 1+ GB RAM.
#  Cloud mode only supports Hinglish → Sarvam AI (cloud API, zero local ML).
#  Uses Mistral embeddings API for RAG — no local sentence-transformer model.
#
#  Local development (full Whisper support): use Requirements.txt instead.
# =============================================================================

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2024.4.9
yt-dlp-ejs>=0.8.0
pydub>=0.25.1
static-ffmpeg>=3.0
ffmpeg-python>=0.2.0

# ── Speech-to-Text — Sarvam AI only (cloud API, no local model) ──────────────
sarvamai>=0.1.0

# ── Translation ───────────────────────────────────────────────────────────────
deep-translator>=1.11.4

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-mistralai>=0.1.0
langchain-chroma>=0.1.0
langsmith>=0.1.0
mistralai>=0.4.0

# ── RAG Pipeline — Mistral Embeddings API (no local model download) ───────────
chromadb>=0.5.0
tiktoken>=0.7.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-extras>=0.4.0
watchdog>=4.0.0
altair>=5.0.0
pillow>=10.0.0
pandas>=2.0.0

# ── PDF Export ────────────────────────────────────────────────────────────────
fpdf2>=2.7.0
reportlab>=4.0.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Async / HTTP ──────────────────────────────────────────────────────────────
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.32.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.26.0
tqdm>=4.66.0
regex>=2024.5.9
rich>=13.0.0
tenacity>=8.2.0
pyyaml>=6.0.0
click>=8.0.0
packaging>=23.0
typing-extensions>=4.8.0
