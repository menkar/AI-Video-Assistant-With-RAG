# =============================================================================
#  MenkarAI — AI Video Assistant with RAG
#  Requirements.txt  (pinned to tested versions; upgrade at your own risk)
#  Python 3.10–3.14  |  Tested on Python 3.14 / Windows 11
# =============================================================================

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2026.6.9                  # Download audio from YouTube URLs
yt-dlp-ejs>=0.8.0                 # YouTube JS challenge solver for yt-dlp
pydub>=0.25.1                     # Audio file manipulation / format conversion
audioop-lts>=0.2.2; python_version>='3.13'  # audioop backport (removed in Py 3.13+)
static-ffmpeg>=3.0                # Bundled ffmpeg/ffprobe binaries (no system install needed)
ffmpeg-python>=0.2.0              # FFmpeg Python bindings

# ── Speech-to-Text (local Whisper) ────────────────────────────────────────────
openai-whisper>=20250625          # OpenAI Whisper — runs fully locally
numba>=0.65.1                     # Required by Whisper; pin to avoid old llvmlite on Py 3.13+
llvmlite>=0.47.0                  # Must match numba
torch>=2.12.1                     # PyTorch backend for Whisper
torchaudio>=2.11.0                # Audio utilities for PyTorch

# ── HuggingFace / Transformers stack ──────────────────────────────────────────
transformers>=5.12.1              # HuggingFace model hub & transformer models
tokenizers>=0.22.2                # Fast tokenizers (required by transformers)
safetensors>=0.8.0                # Safe/fast model-weight serialisation
huggingface-hub>=1.21.0           # Model downloading from HuggingFace Hub
sentence-transformers>=5.6.0      # Sentence embedding models (used in RAG)
scikit-learn>=1.9.0               # ML utilities (used by sentence-transformers)
scipy>=1.18.0                     # Scientific computing (used by sentence-transformers)

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain>=1.3.11
langchain-core>=1.4.8
langchain-community>=0.4.2
langchain-text-splitters>=1.1.2   # Text chunking for RAG pipeline
langchain-mistralai>=1.1.5        # LangChain wrapper for Mistral API
langchain-huggingface>=1.2.2      # LangChain ↔ HuggingFace embeddings bridge
langchain-chroma>=1.1.0           # LangChain ↔ Chroma vector store bridge
langchain-classic>=1.0.8          # LangChain classic chains
langsmith>=0.9.3                  # LangChain tracing / monitoring
langgraph>=1.2.6                  # LangGraph for stateful agent workflows
mistralai>=2.5.0                  # Official Mistral Python client
sarvamai>=0.1.28                  # Sarvam AI Python client (Hinglish STT)

# ── Translation ───────────────────────────────────────────────────────────────
deep-translator>=1.11.4           # Lightweight translation wrapper (Google backend)

# ── RAG Pipeline — Vector Store ───────────────────────────────────────────────
chromadb>=1.5.9                   # Local Chroma vector store
tiktoken>=0.13.0                  # Token counting for text splitting

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.58.0
streamlit-extras>=1.6.0           # Additional Streamlit widgets / utilities
watchdog>=6.0.0                   # File-change watcher (improves hot-reload)
altair>=6.2.2                     # Declarative visualisation (Streamlit dependency)
pillow>=12.2.0                    # Image processing (PIL)
pandas>=3.0.3                     # DataFrame support for Streamlit components

# ── PDF & TXT Export ──────────────────────────────────────────────────────────
fpdf2>=2.8.7                      # Lightweight PDF generation (used in app.py)
reportlab>=5.0.0                  # Full-featured PDF library (alternative)

# ── Data validation & serialisation ───────────────────────────────────────────
pydantic>=2.13.4                  # Data validation (used by LangChain & FastAPI)
pydantic-settings>=2.14.2         # Settings management via pydantic

# ── Async / HTTP ──────────────────────────────────────────────────────────────
aiohttp>=3.14.1                   # Async HTTP client (used by LangChain, Sarvam)
httpx>=0.28.1                     # Sync/async HTTP client (Mistral, HuggingFace)
requests>=2.34.2                  # Standard HTTP library

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.2.2              # Load API keys from .env
numpy>=2.4.6                      # Numerical computing
tqdm>=4.68.3                      # Progress bars
regex>=2026.5.9                   # Enhanced regex (used by tiktoken, transformers)
rich>=15.0.0                      # Rich terminal output / logging
colorama>=0.4.6                   # Windows terminal colour support
joblib>=1.5.3                     # Parallel processing utilities
tenacity>=9.1.4                   # Retry logic for API calls
pyyaml>=6.0.3                     # YAML parsing
click>=8.4.2                      # CLI framework (used by many tools)
packaging>=26.2                   # Package version parsing
typing-extensions>=4.15.0         # Backported typing hints
