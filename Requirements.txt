# =============================================================================
#  MenkarAI — AI Video Assistant with RAG
#  Requirements.txt — Python 3.11 compatible (Render / Linux deployment)
#  Local dev: Python 3.14 / Windows 11
# =============================================================================

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2024.4.9
yt-dlp-ejs>=0.8.0
pydub>=0.25.1
audioop-lts>=0.2.2; python_version>='3.13'
static-ffmpeg>=3.0
ffmpeg-python>=0.2.0

# ── Speech-to-Text (local Whisper) ────────────────────────────────────────────
openai-whisper>=20231117
numba>=0.60.0
llvmlite>=0.43.0
# NOTE: torch is installed separately via PyTorch CPU index in render.yaml
# to avoid downloading the much larger CUDA build on Render's CPU-only instances
torch>=2.0.0
torchaudio>=2.0.0

# ── HuggingFace / Transformers stack ──────────────────────────────────────────
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0
huggingface-hub>=0.23.0
sentence-transformers>=3.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
numpy>=1.26.0

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-mistralai>=0.1.0
langchain-huggingface>=0.0.3
langchain-chroma>=0.1.0
langchain-classic>=1.0.0
langsmith>=0.1.0
langgraph>=0.2.0
mistralai>=0.4.0
sarvamai>=0.1.0

# ── Translation ───────────────────────────────────────────────────────────────
deep-translator>=1.11.4

# ── RAG Pipeline — Vector Store ───────────────────────────────────────────────
chromadb>=0.5.0
tiktoken>=0.7.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-extras>=0.4.0
watchdog>=4.0.0
altair>=5.0.0
pillow>=10.0.0
pandas>=2.0.0

# ── PDF Export ────────────────────────────────────────────────────────────────
fpdf2>=2.7.0
reportlab>=4.0.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Async / HTTP ──────────────────────────────────────────────────────────────
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.32.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
regex>=2024.5.9
rich>=13.0.0
colorama>=0.4.6
joblib>=1.3.0
tenacity>=8.2.0
pyyaml>=6.0.0
click>=8.0.0
packaging>=23.0
typing-extensions>=4.8.0
